version: '3.8'

services:
  app:
    build: .
    restart: always
    network_mode: host
    environment:
      DB_HOST: 127.0.0.1
      DB_USER: root
      DB_PASSWORD: adminroot
      DB_NAME: test
